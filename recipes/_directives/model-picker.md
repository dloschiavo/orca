
If you have picked an agent who is not you, estimate complexity of this story for which model to use:

{models.list}

Choose a model based on the appropriateness of the complexity of this story:
  - higher / more complex: cross-cutting architectural work, new subsystem design, complex multi-step refactors, tricky concurrency/state issues, or anything that requires reasoning about non-local invariants.
  - medium: multi-file but straightforward — adding a new route with DB query, updating a component and its tests, wiring an existing API to a new UI.
  - lower / simpler: single-file change, simple bug fix, text/config tweak, adding a suppression rule, renaming, or other mechanical edits.

The model is the actual model string.